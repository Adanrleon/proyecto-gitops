apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: github-eventsource
  namespace: argo
spec:
  webhook:
    git-webhook:
      port: "12000"
      endpoint: /push
      method: POST