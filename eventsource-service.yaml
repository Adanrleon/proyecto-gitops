apiVersion: v1
kind: Service
metadata:
  name: github-eventsource-service
  namespace: argo
spec:
  selector:
    eventsource-name: github-eventsource
    controller: eventsource-controller
  ports:
  - port: 12000
    targetPort: 12000
    name: webhook