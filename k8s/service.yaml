apiVersion: v1
kind: Service
metadata:
  name: app-gitops-service
spec:
  # LoadBalancer en Docker Desktop expone la app directamente en tu localhost
  type: LoadBalancer 
  selector:
    app: app-gitops # Esto conecta este servicio con el deployment de arriba
  ports:
    - port: 80
      targetPort: 80